import React from 'react'

const UserResponse = (): JSX.Element => {
  // const dispatch = useDispatch()
  // const popup = usePopUp()
  // const { post } = usePostDetails()
  // const handleLikeOrDislike = type => () => {
  // const key = `${type}s`
  // const action = post[key].includes(user.userId) ? 'REMOVE' : 'ADD'
  // if (user.userId) {
  //   API.posts
  //     .addLikeOrDislike(post.postId, { action: `${action}_${type}`.toUpperCase() })
  //     .then(post => dispatch(setPost(post)))
  // } else {
  //   // popup.onOpen('clap on story')
  // }
  // }
  // const comments: string[] = []
  //
  // return <UserActivity likes={[]} mt={2} mb={2} dislikes={[]} handleLikeOrDislike={() => ({})} list={comments} />
  return <></>
}

export default UserResponse
